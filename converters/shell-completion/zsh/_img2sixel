#compdef img2sixel
# ------------------------------------------------------------------------------
# Description
# -----------
#
#  Completion script for img2sixel (https://github.com/saitoha/libsixel)
#
# ------------------------------------------------------------------------------
# Author
# -------
#
#  * Hayaki Saito (https://github.com/saitoha)
#
# ------------------------------------------------------------------------------

_diffusiontype() {
  _values \
    'DIFFUSIONTYPE' \
    'auto[choose diffusion type automatically (default)]' \
    'none[do not diffuse]' \
    'fs[Floyd-Steinberg method]' \
    "atkinson[Bill Atkinson's method]" \
    'jajuni[Jarvis, Judice & Ninke method]' \
    "stucki[Stucki's method]" \
    "burkes[Burkes' method]" \
    'sierra[Sierra diffusion family (default variant=1)]' \
    'sierra:variant=1[Sierra Lite method]' \
    'sierra:variant=2[Sierra Two-row method]' \
    'sierra:variant=3[Sierra-3 method]' \
    'fs:scan=serpentine[Floyd-Steinberg with serpentine scan]' \
    'sierra:variant=2:scan=serpentine[Sierra Two-row with serpentine scan]' \
    'a_dither[positionally stable arithmetic dither]' \
    'x_dither[positionally stable arithmetic xor based dither]' \
    'bluenoise[tileable blue-noise ordered dither]' \
    'bluenoise:strength=0.055[bluenoise amplitude override]' \
    'bluenoise:gradient_factor=1.0[bluenoise gradient attenuation gamma]' \
    'bluenoise:G1.0[short alias for bluenoise gradient attenuation gamma]' \
    'bluenoise:phase=10,20[bluenoise tile phase override]' \
    'bluenoise:seed=123[bluenoise phase seed override]' \
    'bluenoise:channel=mono[bluenoise mono channel mode]' \
    'bluenoise:channel=rgb[bluenoise per-channel rgb mode]' \
    'bluenoise:size=64[bluenoise tile size (embedded)]' \
    'bluenoise:channel=rgb:scan=serpentine[rgb channel mode + scan override]' \
    'lso2[libsixel method based on variable error diffusion]' \
    'interframe[interframe error diffusion (palette path only)]' \
    'interframe:scan=serpentine[interframe with serpentine scan]' \
    'interframe:diffusion=atkinson[interframe with Atkinson kernel]' \
    'interframe:diffusion=sierra2[interframe with Sierra Two-row kernel]' \
    'interframe:diffusion=sierra2:scan=serpentine[interframe kernel + scan override]' \
    'stbn[interframe STBN source selector (default source=hash)]' \
    'stbn:scan=serpentine[STBN with serpentine scan override]' \
    'stbn:diffusion=atkinson[STBN with Atkinson kernel]' \
    'stbn:source=hash[STBN hash source]' \
    'stbn:source=mask[STBN mask-table source]' \
    'stbn:source=pmj[PMJ source]' \
    'stbn:source=mask:scan=serpentine[mask source + serpentine scan]' \
    'stbn:source=mask:diffusion=sierra2[mask source + Sierra Two-row]' \
    'stbn:source=pmj:diffusion=atkinson[pmj source + Atkinson]' \
    'stbn:source=pmj:diffusion=atkinson:scan=serpentine[pmj source + Atkinson + scan override]' \
    'stbn:strength=0.055[STBN/PMJ noise strength override]' \
    'stbn:source=hash:strength=0.055[hash source + noise override]' \
    'stbn:source=mask:strength=0.50[mask source + noise override]' \
    'stbn:source=pmj:strength=0.50[pmj source + noise override]' \
    'stbn:motion_adapt=1[enable motion adaptive stbn strength]' \
    'stbn:scene_cut_reset=1[reset stbn carry at frame boundaries]' \
    'stbn:scene_detect=1[reset stbn carry on detected scene changes]' \
    'stbn:alpha_guard=1[suppress stbn near alpha edges]' \
    'stbn:perceptual_weight=1[enable perceptual channel weighting]' \
    'stbn:fastpath=1[enable pmj fast path]'
}

_findtype() {
  _values \
    'FINDTYPE' \
    'auto[choose finding method automatically (default)]' \
    'norm[simply comparing the range in RGB space]' \
    'lum[transforming into luminosities before the comparison]' \
    'pca[split along the first principal component and cut at weighted median]'
}

_selecttype() {
  _values \
    'SELECTTYPE' \
    'auto[choose selecting method automatically (default)]' \
    'center[choose the center of the box]' \
    'average[calculate the color average into the box]' \
    'histogram[similar with average but considers color histogram]'
}

_resamplingtype() {
  _values \
    'RESAMPLINGTYPE' \
    'nearest[Nearest-Neighbor method]' \
    'gaussian[Gaussian filter]' \
    'hanning[Hanning filter]' \
    'hamming[Hamming filter]' \
    'bilinear[Bilinear filter (default)]' \
    'welsh[Welsh filter]' \
    'bicubic[Bicubic filter]' \
    'lanczos2[Lanczos2 filter]' \
    'lanczos3[Lanczos3 filter]' \
    'lanczos4[Lanczos4 filter]'
}

_qualitytype() {
  _values \
    'QUALITYTYPE' \
    'auto[decide quality mode automatically (default)]' \
    'high[high quality and low speed mode]' \
    'low[low quality and high speed mode]' \
    'full[full quality mode]'
}

_lookuppolicy() {
  _values \
    'LOOKUPPOLICY' \
    'auto[follow pixel depth to pick histogram width]' \
    '5bit[force 5-bit buckets]' \
    '6bit[force 6-bit buckets for RGB inputs]' \
    'none[disable LUT caching and scan directly]' \
    'certlut[certified hierarchical LUT with zero error]' \
    'eytzinger[implicit binary tree lookup with neighbour scan]' \
    'fhedt[Voronoi grid built via a 3D EDT with boundary refinement]' \
    'vptree[VP-tree lookup built from palette entries]' \
    'rbc[Random Ball Cover cluster pruning]' \
    'mahalanobis[RBC pruning with Mahalanobis lower bounds]'
}

_looptype() {
  _values \
    'LOOPTYPE' \
    'auto[honor the setting of GIF header (default)]' \
    'force[always enable loop]' \
    'disable[always disable loop]'
}

_palettetype() {
  _values \
    'PALETTETYPE' \
    'auto[choose palette type automatically (default)]' \
    'hls[use HLS color space]' \
    'rgb[use RGB color space]'
}

_builtinpalette() {
  _values \
    'BUILTINPALETTE' \
    'xterm16[X default 16 color map]' \
    'xterm256[X default 256 color map]' \
    'vt340mono[VT340 monochrome map]' \
    'vt340color[VT340 color map]' \
    'gray1[1bit grayscale map]' \
    'gray2[2bit grayscale map]' \
    'gray4[4bit grayscale map]' \
    'gray8[8bit grayscale map]'
}

_encodepolicy() {
  _values \
    'ENCODEPOLICY' \
    'auto[choose encoding policy automatically (default)]' \
    'fast[encode as fast as possible]' \
    'size[encode to as small sixel sequence as possible]'
}

_cmsengine() {
  _values \
    'CMSENGINE' \
    'none[disable loader CMS conversion]' \
    'auto[prefer lcms2, then ColorSync (macOS), then builtin]' \
    'builtin[force builtin CMS backend]' \
    'lcms2[force lcms2 CMS backend]' \
    'colorsync[force ColorSync CMS backend]'
}

_loaders() {
  _values -s , \
    'LOADER' \
    'libpng[libpng based loader]' \
    'libjpeg[libjpeg based loader]' \
    'libwebp[libwebp based loader]' \
    'libtiff[libtiff based loader]' \
    'librsvg[librsvg based loader]' \
    'builtin[libsixel builtin decoder]' \
    'wic[Windows Imaging Component loader]' \
    'coregraphics[macOS CoreGraphics loader]' \
    'gd[libgd based loader]' \
    'gdk-pixbuf2[gdk-pixbuf2 image loader]' \
    'gnome-thumbnailer[external thumbnailer helper]' \
    'quicklook[macOS Quick Look integration]'
}

_clusteringcolorspace() {
  _values \
    'COLORSPACE' \
    'gamma[keep gamma encoded pixels (default)]' \
    'linear[use linear RGB for palette clustering]' \
    'oklab[cluster in OKLab uniform color space]' \
    'cielab[cluster in CIELAB uniform color space]' \
    'din99d[cluster in DIN99d uniform color space]'
}

_workingcolorspace() {
  _values \
    'WORKINGCOLORSPACE' \
    'gamma[keep gamma encoded pixels (default)]' \
    'linear[use linear RGB as working space]' \
    'oklab[operate in OKLab uniform color space]' \
    'cielab[operate in CIELAB uniform color space]' \
    'din99d[operate in DIN99d uniform color space]'
}

_outputcolorspace() {
  _values \
    'OUTPUTCOLORSPACE' \
    'gamma[sRGB gamma encoded output (default)]' \
    'linear[linear RGB output]' \
    'smpte-c[SMPTE-C gamma encoded output]'
}

_quantizemodel() {
  local current base segment key prefix

  current=${words[CURRENT]}
  if [[ ${current} == *:* ]]; then
    base=${current%%:*}
    segment=${current##*:}
    if [[ ${base} == auto || ${base} == auto* || \
          ${base} == heckbert || ${base} == heckbert* ]]; then
      if [[ ${segment} == *=* ]]; then
        key=${segment%%=*}
        prefix=${current%${segment}}
        if [[ ${key} == merge ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=ward"
          return 0
        elif [[ ${key} == animation_mode ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1"
          return 0
        elif [[ ${key} == scene_cut_threshold ]]; then
          compadd -- "${prefix}${key}=0.15" \
                     "${prefix}${key}=0.20" \
                     "${prefix}${key}=0.30"
          return 0
        elif [[ ${key} == merge_oversplit ]]; then
          compadd -- "${prefix}${key}=1.2" \
                     "${prefix}${key}=1.5" \
                     "${prefix}${key}=1.8" \
                     "${prefix}${key}=2.0"
          return 0
        elif [[ ${key} == merge_lloyd ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=8"
          return 0
        fi
      else
        prefix=${current%${segment}}
        compadd -- "${prefix}animation_mode=" \
                   "${prefix}scene_cut_threshold=" \
                   "${prefix}Gauto" \
                   "${prefix}Gnone" \
                   "${prefix}Gward" \
                   "${prefix}merge=" \
                   "${prefix}O1.2" \
                   "${prefix}O1.5" \
                   "${prefix}O2.0" \
                   "${prefix}merge_oversplit=" \
                   "${prefix}L0" \
                   "${prefix}L1" \
                   "${prefix}L3" \
                   "${prefix}L5" \
                   "${prefix}merge_lloyd="
        return 0
      fi
    elif [[ ${base} == k || ${base} == kmeans* ]]; then
      if [[ ${segment} == *=* ]]; then
        key=${segment%%=*}
        prefix=${current%${segment}}
        if [[ ${key} == inittype ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=pca"
          return 0
        elif [[ ${key} == binning ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=hard" \
                     "${prefix}${key}=soft"
          return 0
        elif [[ ${key} == mapping ]]; then
          compadd -- "${prefix}${key}=uniform" \
                     "${prefix}${key}=srgb"
          return 0
        elif [[ ${key} == softdist ]]; then
          compadd -- "${prefix}${key}=trilinear"
          return 0
        elif [[ ${key} == binbits ]]; then
          compadd -- "${prefix}${key}=4" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=6" \
                     "${prefix}${key}=7" \
                     "${prefix}${key}=8"
          return 0
        elif [[ ${key} == autoratio ]]; then
          compadd -- "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=32" \
                     "${prefix}${key}=64"
          return 0
        elif [[ ${key} == feedback ]]; then
          compadd -- "${prefix}${key}=off" \
                     "${prefix}${key}=on"
          return 0
        elif [[ ${key} == seed ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=7" \
                     "${prefix}${key}=42" \
                     "${prefix}${key}=256"
          return 0
        elif [[ ${key} == restarts ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == iter || ${key} == iter_max ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=32" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=100"
          return 0
        elif [[ ${key} == miniter ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=32" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=100"
          return 0
        elif [[ ${key} == polish_iter ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == feedback_slots ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == feedback_interval ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=32" \
                     "${prefix}${key}=64"
          return 0
        elif [[ ${key} == prune ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=hamerly" \
                     "${prefix}${key}=elkan" \
                     "${prefix}${key}=yinyang"
          return 0
        elif [[ ${key} == animation_mode ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1"
          return 0
        elif [[ ${key} == scene_cut_threshold ]]; then
          compadd -- "${prefix}${key}=0.15" \
                     "${prefix}${key}=0.20" \
                     "${prefix}${key}=0.30"
          return 0
        elif [[ ${key} == merge ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=ward"
          return 0
        elif [[ ${key} == merge_oversplit ]]; then
          compadd -- "${prefix}${key}=1.2" \
                     "${prefix}${key}=1.5" \
                     "${prefix}${key}=1.8" \
                     "${prefix}${key}=2.0"
          return 0
        elif [[ ${key} == merge_lloyd ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=8"
          return 0
        fi
      else
        prefix=${current%${segment}}
        compadd -- "${prefix}Iauto" "${prefix}Inone" \
                   "${prefix}Ipca" "${prefix}inittype=" \
                   "${prefix}T0.120" "${prefix}threshold=" \
                   "${prefix}Bauto" "${prefix}Bhard" \
                   "${prefix}Bsoft" "${prefix}binning=" \
                   "${prefix}N4" "${prefix}N6" \
                   "${prefix}N8" "${prefix}binbits=" \
                   "${prefix}Muniform" "${prefix}Msrgb" \
                   "${prefix}mapping=" \
                   "${prefix}Dtrilinear" "${prefix}softdist=" \
                   "${prefix}R32" "${prefix}autoratio=" \
                   "${prefix}Foff" "${prefix}Fon" \
                   "${prefix}feedback=" \
                   "${prefix}prune=" \
                   "${prefix}S1" "${prefix}S42" \
                   "${prefix}seed=" \
                   "${prefix}restarts=" \
                   "${prefix}iter=" "${prefix}iter_max=" \
                   "${prefix}miniter=" "${prefix}polish_iter=" \
                   "${prefix}feedback_slots=" \
                   "${prefix}feedback_interval=" \
                   "${prefix}animation_mode=" \
                   "${prefix}scene_cut_threshold=" \
                   "${prefix}Gauto" \
                   "${prefix}Gnone" \
                   "${prefix}Gward" \
                   "${prefix}merge=" \
                   "${prefix}O1.2" \
                   "${prefix}O1.5" \
                   "${prefix}O2.0" \
                   "${prefix}merge_oversplit=" \
                   "${prefix}L0" \
                   "${prefix}L1" \
                   "${prefix}L3" \
                   "${prefix}L5" \
                   "${prefix}merge_lloyd="
        return 0
      fi
    elif [[ ${base} == medoids* ]]; then
      if [[ ${segment} == *=* ]]; then
        key=${segment%%=*}
        prefix=${current%${segment}}
        if [[ ${key} == algo ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=pam" \
                     "${prefix}${key}=sample" \
                     "${prefix}${key}=random" \
                     "${prefix}${key}=bandit"
          return 0
        elif [[ ${key} == seed ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=7" \
                     "${prefix}${key}=42" \
                     "${prefix}${key}=256"
          return 0
        elif [[ ${key} == iter ]]; then
          compadd -- "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=24"
          return 0
        elif [[ ${key} == histbits ]]; then
          compadd -- "${prefix}${key}=3" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=6"
          return 0
        elif [[ ${key} == sample || ${key} == clara_sample ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1024" \
                     "${prefix}${key}=2048" \
                     "${prefix}${key}=4096"
          return 0
        elif [[ ${key} == point_budget ]]; then
          compadd -- "${prefix}${key}=64" \
                     "${prefix}${key}=256" \
                     "${prefix}${key}=1024" \
                     "${prefix}${key}=4096" \
                     "${prefix}${key}=16384"
          return 0
        elif [[ ${key} == rare_keep ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=128" \
                     "${prefix}${key}=256"
          return 0
        elif [[ ${key} == prune_mass ]]; then
          compadd -- "${prefix}${key}=0.900" \
                     "${prefix}${key}=0.950" \
                     "${prefix}${key}=0.980" \
                     "${prefix}${key}=0.995" \
                     "${prefix}${key}=1.000"
          return 0
        elif [[ ${key} == auction ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1"
          return 0
        elif [[ ${key} == auction_shortlist ]]; then
          compadd -- "${prefix}${key}=2" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=6" \
                     "${prefix}${key}=8"
          return 0
        elif [[ ${key} == clara_trials || ${key} == clarans_local ]]; then
          compadd -- "${prefix}${key}=3" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=7" \
                     "${prefix}${key}=9"
          return 0
        elif [[ ${key} == clarans_neighbors ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=250" \
                     "${prefix}${key}=512" \
                     "${prefix}${key}=1024"
          return 0
        elif [[ ${key} == bandit_iter ]]; then
          compadd -- "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=12" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == bandit_candidates || ${key} == bandit_batch ]]; then
          compadd -- "${prefix}${key}=32" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=128" \
                     "${prefix}${key}=256" \
                     "${prefix}${key}=512"
          return 0
        elif [[ ${key} == animation_mode ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1"
          return 0
        elif [[ ${key} == scene_cut_threshold ]]; then
          compadd -- "${prefix}${key}=0.15" \
                     "${prefix}${key}=0.20" \
                     "${prefix}${key}=0.30"
          return 0
        elif [[ ${key} == merge ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=ward"
          return 0
        elif [[ ${key} == merge_oversplit ]]; then
          compadd -- "${prefix}${key}=1.2" \
                     "${prefix}${key}=1.5" \
                     "${prefix}${key}=1.8" \
                     "${prefix}${key}=2.0"
          return 0
        elif [[ ${key} == merge_lloyd ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=8"
          return 0
        fi
      else
        prefix=${current%${segment}}
        compadd -- "${prefix}Aauto" "${prefix}Apam" \
                   "${prefix}Asample" "${prefix}Arandom" \
                   "${prefix}Abandit" "${prefix}algo=" \
                   "${prefix}S1" "${prefix}S42" \
                   "${prefix}seed=" \
                   "${prefix}iter=" "${prefix}sample=" \
                   "${prefix}histbits=" \
                   "${prefix}point_budget=" \
                   "${prefix}rare_keep=" \
                   "${prefix}prune_mass=" \
                   "${prefix}auction=" \
                   "${prefix}auction_shortlist=" \
                   "${prefix}clara_trials=" "${prefix}clara_sample=" \
                   "${prefix}clarans_local=" \
                   "${prefix}clarans_neighbors=" \
                   "${prefix}bandit_iter=" \
                   "${prefix}bandit_candidates=" \
                   "${prefix}bandit_batch=" \
                   "${prefix}animation_mode=" \
                   "${prefix}scene_cut_threshold=" \
                   "${prefix}Gauto" \
                   "${prefix}Gnone" \
                   "${prefix}Gward" \
                   "${prefix}merge=" \
                   "${prefix}O1.2" \
                   "${prefix}O1.5" \
                   "${prefix}O2.0" \
                   "${prefix}merge_oversplit=" \
                   "${prefix}L0" \
                   "${prefix}L1" \
                   "${prefix}L3" \
                   "${prefix}L5" \
                   "${prefix}merge_lloyd="
        return 0
      fi
    elif [[ ${base} == c || ${base} == center* ]]; then
      if [[ ${segment} == *=* ]]; then
        key=${segment%%=*}
        prefix=${current%${segment}}
        if [[ ${key} == algo ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=fft" \
                     "${prefix}${key}=swap" \
                     "${prefix}${key}=hybrid"
          return 0
        elif [[ ${key} == profile ]]; then
          compadd -- "${prefix}${key}=legacy" \
                     "${prefix}${key}=speed" \
                     "${prefix}${key}=balance" \
                     "${prefix}${key}=quality"
          return 0
        elif [[ ${key} == auto_policy ]]; then
          compadd -- "${prefix}${key}=legacy" \
                     "${prefix}${key}=adaptive"
          return 0
        elif [[ ${key} == auto_fft_threshold ]]; then
          compadd -- "${prefix}${key}=256" \
                     "${prefix}${key}=1024" \
                     "${prefix}${key}=2048" \
                     "${prefix}${key}=4096" \
                     "${prefix}${key}=8192"
          return 0
        elif [[ ${key} == space_policy ]]; then
          compadd -- "${prefix}${key}=legacy" \
                     "${prefix}${key}=perceptual"
          return 0
        elif [[ ${key} == candidate_policy ]]; then
          compadd -- "${prefix}${key}=legacy" \
                     "${prefix}${key}=hybrid"
          return 0
        elif [[ ${key} == seed ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=7" \
                     "${prefix}${key}=42" \
                     "${prefix}${key}=256"
          return 0
        elif [[ ${key} == restarts ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == init_seeds ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=6" \
                     "${prefix}${key}=8"
          return 0
        elif [[ ${key} == iter ]]; then
          compadd -- "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=24" \
                     "${prefix}${key}=32" \
                     "${prefix}${key}=64"
          return 0
        elif [[ ${key} == histbits ]]; then
          compadd -- "${prefix}${key}=3" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=6"
          return 0
        elif [[ ${key} == point_budget ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=256" \
                     "${prefix}${key}=1024" \
                     "${prefix}${key}=4096" \
                     "${prefix}${key}=16384"
          return 0
        elif [[ ${key} == rare_keep ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=16" \
                     "${prefix}${key}=64" \
                     "${prefix}${key}=128" \
                     "${prefix}${key}=256" \
                     "${prefix}${key}=512" \
                     "${prefix}${key}=1024" \
                     "${prefix}${key}=2048"
          return 0
        elif [[ ${key} == prune_mass ]]; then
          compadd -- "${prefix}${key}=0.900" \
                     "${prefix}${key}=0.950" \
                     "${prefix}${key}=0.980" \
                     "${prefix}${key}=0.995" \
                     "${prefix}${key}=1.000"
          return 0
        elif [[ ${key} == budget_policy ]]; then
          compadd -- "${prefix}${key}=legacy" \
                     "${prefix}${key}=adaptive"
          return 0
        elif [[ ${key} == budget_scale ]]; then
          compadd -- "${prefix}${key}=0.50" \
                     "${prefix}${key}=0.80" \
                     "${prefix}${key}=1.00" \
                     "${prefix}${key}=1.25" \
                     "${prefix}${key}=1.40"
          return 0
        elif [[ ${key} == swap_topk ]]; then
          compadd -- "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8" \
                     "${prefix}${key}=16"
          return 0
        elif [[ ${key} == swap_update ]]; then
          compadd -- "${prefix}${key}=full" \
                     "${prefix}${key}=incremental"
          return 0
        elif [[ ${key} == swap_patience ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=2" \
                     "${prefix}${key}=4" \
                     "${prefix}${key}=8"
          return 0
        elif [[ ${key} == swap_min_gain ]]; then
          compadd -- "${prefix}${key}=0.00" \
                     "${prefix}${key}=0.02" \
                     "${prefix}${key}=0.05" \
                     "${prefix}${key}=0.10" \
                     "${prefix}${key}=0.25"
          return 0
        elif [[ ${key} == animation_mode ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1"
          return 0
        elif [[ ${key} == scene_cut_threshold ]]; then
          compadd -- "${prefix}${key}=0.15" \
                     "${prefix}${key}=0.20" \
                     "${prefix}${key}=0.30"
          return 0
        elif [[ ${key} == merge ]]; then
          compadd -- "${prefix}${key}=auto" \
                     "${prefix}${key}=none" \
                     "${prefix}${key}=ward"
          return 0
        elif [[ ${key} == merge_oversplit ]]; then
          compadd -- "${prefix}${key}=1.2" \
                     "${prefix}${key}=1.5" \
                     "${prefix}${key}=1.8" \
                     "${prefix}${key}=2.0"
          return 0
        elif [[ ${key} == merge_lloyd ]]; then
          compadd -- "${prefix}${key}=0" \
                     "${prefix}${key}=1" \
                     "${prefix}${key}=3" \
                     "${prefix}${key}=5" \
                     "${prefix}${key}=8"
          return 0
        fi
      else
        prefix=${current%${segment}}
        compadd -- "${prefix}Aauto" "${prefix}Afft" \
                   "${prefix}Aswap" "${prefix}Ahybrid" \
                   "${prefix}algo=" \
                   "${prefix}profile=" \
                   "${prefix}auto_policy=" \
                   "${prefix}auto_fft_threshold=" \
                   "${prefix}space_policy=" \
                   "${prefix}candidate_policy=" \
                   "${prefix}S1" "${prefix}S42" \
                   "${prefix}seed=" \
                   "${prefix}restarts=" \
                   "${prefix}init_seeds=" \
                   "${prefix}iter=" \
                   "${prefix}histbits=" \
                   "${prefix}point_budget=" \
                   "${prefix}rare_keep=" \
                   "${prefix}prune_mass=" \
                   "${prefix}budget_policy=" \
                   "${prefix}budget_scale=" \
                   "${prefix}swap_topk=" \
                   "${prefix}swap_update=" \
                   "${prefix}swap_patience=" \
                   "${prefix}swap_min_gain=" \
                   "${prefix}animation_mode=" \
                   "${prefix}scene_cut_threshold=" \
                   "${prefix}Gauto" \
                   "${prefix}Gnone" \
                   "${prefix}Gward" \
                   "${prefix}merge=" \
                   "${prefix}O1.2" \
                   "${prefix}O1.5" \
                   "${prefix}O2.0" \
                   "${prefix}merge_oversplit=" \
                   "${prefix}L0" \
                   "${prefix}L1" \
                   "${prefix}L3" \
                   "${prefix}L5" \
                   "${prefix}merge_lloyd="
        return 0
      fi
    fi
  fi

  _values \
    'MODEL' \
    'auto[select solver automatically]' \
    'heckbert[Heckbert median-cut solver]' \
    'kmeans[k-means palette solver]' \
    'medoids[k-medoids palette solver]' \
    'center[discrete k-center palette solver]'
}


_completiontarget() {
  _values \
    'TARGET' \
    'all[manage both bash and zsh completions]' \
    'bash[operate on bash completions only]' \
    'zsh[operate on zsh completions only]'
}

_threadcount() {
  _values \
    'THREADS' \
    'auto[match the hardware thread count]'
}

_precisionmode() {
  _values \
    'PRECISIONMODE' \
    'auto[defer to SIXEL_FLOAT32_DITHER or the 8-bit default]' \
    '8bit[force the legacy integer pipeline]' \
    'float32[opt into the experimental high-precision pipeline]'
}

# Keep the following option order synchronized with `img2sixel -H`.
_arguments -S -s -A "-*" -S \
  {-o,--outfile}'[specify output file (default: stdout)]':files:_files \
  {-=,--threads=}'[override encoder thread count (integer or auto)]':threadcount:_threadcount \
  {-.,--precision=}'[control quantization precision]':precisionmode:_precisionmode \
  {-7,--7bit-mode}'[generate a sixel image for 7bit terminals (default)]' \
  {-8,--8bit-mode}'[generate a sixel image for 8bit terminals]' \
  {-R,--gri-limit}'[limit arguments of DECGRI(!) to 255]' \
  {-6,--6reversible}'[snap the palette to SIXEL-reversible tones]' \
  {-p,--colors=}'[specify number of colors to reduce the image to]' \
  {-Q,--quantize-model=}'[choose palette solver]':quantizemodel:_quantizemodel \
  {-m,--mapfile=}'[transform image colors using a map image or ACT/PAL/GPL palette; TYPE:PATH prefixes (TYPE:- for stdin) allowed]':files:_files \
  {-M,--mapfile-output=}'[export the computed palette; TYPE:PATH (TYPE:- for stdout) or .act/.pal/.gpl extensions choose the format]':files:_files \
  {-e,--monochrome}'[output monochrome sixel image]' \
  {-k,--insecure}'[allow to connect to SSL sites without certs]' \
  {-i,--invert}'[assume the terminal background color is white]' \
  {-I,--high-color}'[output 15bpp sixel image]' \
  {-u,--use-macro}'[use DECDMAC and DECINVM for GIF animation]' \
  {-n,--macro-number}'[specify a number argument for DECDMAC]' \
  {-C,--complexion-score=}'[deprecated; accepted but ignored]' \
  {-g,--ignore-delay}'[render GIF animation without delay]' \
  {-S,--static}'[render animated GIF as a static image]' \
  {-d,--diffusion=}'[choose diffusion method which used with -p option]':diffusiontype:_diffusiontype \
  {-f,--find-largest=}'[method for finding the largest dimension in median-cut]':findtype:_findtype \
  {-s,--select-color=}'[method for selecting color from median-cut boxes]':selecttype:_selecttype \
  {-c,--crop=}'[crop image to specified geometory(%dx%d+%d+%d)]' \
  {-w,--width=}'[resize image width (auto, numeric, percent, pixel, terminal cell)]' \
  {-h,--height=}'[resize image height (auto, numeric, percent, pixel, terminal cell)]' \
  {-r,--resampling=}'[choose resampling filter used with -w or -h option]':resamplingtype:_resamplingtype \
  {-q,--quality=}'[select quality of color quanlization]':qualitytype:_qualitytype \
  {-~,--lookup-policy=}'[choose histogram lookup width and backend]':lookuppolicy:_lookuppolicy \
  {-l,--loop-control=}'[select loop control mode of GIF animation]':looptype:_looptype \
  {-T,--start-frame=}'[set animation start frame index for loaders]' \
  {-t,--palette-type=}'[select palette color space type]':palettetype:_palettetype \
  {-b,--builtin-palette=}'[select built-in palette type]':builtinpalette:_builtinpalette \
  {-E,--encode-policy=}'[select encoding policy]':encodepolicy:_encodepolicy \
  {-B,--bgcolor=}'[select background color]' \
  {-P,--penetrate}'[penetrate GNU Screen using DCS pass-through sequence]' \
  {-D,--pipe-mode}'[read source images from stdin continuously]' \
  {-v,--verbose}'[show debugging information]' \
  {-L,--loaders=}'[override loader priority order]':loaders:_loaders \
  {-#,--cms-engine=}'[set default loader CMS backend]':cmsengine:_cmsengine \
  {-@,--drcs}'[emit DRCS tiles via MMV:CHARSET:PATH (defaults 2:1:, "-" keeps stdout)]' \
  {-O,--ormode}'[output ormode sixel image]' \
  {-X,--clustering-colorspace=}'[select palette clustering color space (ignored with -b, -m, or -e)]':colorspace:_clusteringcolorspace \
  {-W,--working-colorspace=}'[select internal working color space]':workingcolorspace:_workingcolorspace \
  {-U,--output-colorspace=}'[select output color space]':outputcolorspace:_outputcolorspace \
  {-1,--show-completion=}'[print shell completion script]':target:_completiontarget \
  {-2,--install-completion=}'[install shell completion script]':target:_completiontarget \
  {-3,--uninstall-completion=}'[remove installed shell completion script]':target:_completiontarget \
  {-%,--env=}'[set process environment variable (KEY=VALUE)]' \
  {-V,--version}'[show version and license information]' \
  {-H,--help}'[show help]' \
  '*:files:_files' \
  && return 0

return 1

# Local Variables:
# mode: Shell-Script
# sh-indentation: 2
# indent-tabs-mode: nil
# sh-basic-offset: 2
# End:
# vim: ft=zsh sw=2 ts=2 et
