name: libsixel experimental CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: experimental-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  # Pin vcpkg to a stable release to avoid fetching future tool builds that
  # depend on unavailable CMake versions during bootstrap.
  VCPKG_REF: 2024.12.16
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1

jobs:
  vcpkg-msvc-autotools:
    name: ${{ matrix.label }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - label: Autotools-msys2-msvc-vcpkg-png-jpeg-tiff-webp-no-lcms2
            buildtool: autotools
            runner: windows-latest
            msystem: MSYS
            setup_options: >-
              --host=x86_64-pc-windows-msvc
              --with-png
              --with-jpeg
              --with-tiff
              --with-webp
              --without-lcms2
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-libcurl
              --without-libfetch
            extra_packages: make
            vcpkg_packages: >-
              libpng
              libjpeg-turbo
              tiff
              libwebp
            exec_type: msys2

          - label: Autotools-msys2-msvc-vcpkg-no-png-jpeg-tiff-webp-lcms2
            buildtool: autotools
            runner: windows-latest
            msystem: MSYS
            setup_options: >-
              --host=x86_64-pc-windows-msvc
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --with-lcms2
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-libcurl
              --without-libfetch
            extra_packages: make
            vcpkg_packages: lcms
            exec_type: msys2

          - label: Autotools-msys2-msvc-vcpkg-png-jpeg-tiff-webp-lcms2
            buildtool: autotools
            runner: windows-latest
            msystem: MSYS
            setup_options: >-
              --host=x86_64-pc-windows-msvc
              --with-png
              --with-jpeg
              --with-tiff
              --with-webp
              --with-lcms2
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-libcurl
              --without-libfetch
            extra_packages: make
            vcpkg_packages: >-
              libpng
              libjpeg-turbo
              tiff
              libwebp
              lcms
            exec_type: msys2

          - label: Meson-msys2-msvc-vcpkg-png-jpeg-tiff-webp-lcms2
            buildtool: meson
            runner: windows-latest
            msystem: MSYS
            setup_options: >-
              --native-file build-aux/meson-native/msvc.ini
              --native-file build-aux/meson-native/msvc-vcpkg.ini
              -Dpng=enabled
              -Djpeg=enabled
              -Dtiff=enabled
              -Dwebp=enabled
              -Dlcms2=enabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              make
              meson
              ninja
            vcpkg_packages: >-
              libpng
              libjpeg-turbo
              tiff
              libwebp
              lcms
            exec_type: msys2

    steps:
      - name: Set Windows Defender ExclusionPath (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          try {
            Add-MpPreference -ExclusionPath "C:\a" -ErrorAction Stop
          } catch {
            Write-Warning "Windows Defender exclusion setup failed; continuing: $($_.Exception.Message)"
          }

      - name: Disable Windows Search Indexer (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Stop-Service WSearch

      - name: Configure git EOL
        if: ${{ runner.os == 'Windows' }}
        shell: bash
        run: git config --global core.autocrlf false

      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  FunctionSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-FunctionSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=function
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-FunctionSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=function
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  ImplicitIntegerTruncationSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-ImplicitIntegerTruncationSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=implicit-integer-truncation
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-ImplicitIntegerTruncationSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=implicit-integer-truncation
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  ImplicitIntegerSignChangeSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-ImplicitIntegerSignChangeSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=implicit-integer-sign-change
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-ImplicitIntegerSignChangeSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=implicit-integer-sign-change
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  UnsignedIntegerOverflowSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-UnsignedIntegerOverflowSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=unsigned-integer-overflow
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-UnsignedIntegerOverflowSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=unsigned-integer-overflow
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  LocalBoundsSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-LocalBoundsSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=local-bounds
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-LocalBoundsSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=local-bounds
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  PointerOverflowSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-PointerOverflowSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=pointer-overflow
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-PointerOverflowSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=pointer-overflow
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  FloatCastOverflowSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-FloatCastOverflowSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=float-cast-overflow
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-FloatCastOverflowSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=float-cast-overflow
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  CFIICallSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-CFIICallSan-lto-thin
            buildtool: autotools
            setup_prefix: >-
              CC=clang
              AR=llvm-ar
              RANLIB=llvm-ranlib
              LDFLAGS='-fuse-ld=lld'
            setup_options: >-
              --disable-shared
              --enable-static
              --enable-lto=thin
              --enable-sanitizer=cfi-icall
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              lld
              llvm
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-CFIICallSan-lto-thin
            buildtool: meson
            setup_prefix: >-
              CC=clang
              AR=llvm-ar
              RANLIB=llvm-ranlib
              LDFLAGS='-fuse-ld=lld'
            setup_options: >-
              --buildtype=debugoptimized
              -Ddefault_library=static
              -Db_lto=true
              -Db_lto_mode=thin
              -Dexperimental_sanitizer=cfi-icall
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              lld
              llvm
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  ObjectSizeSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-ObjectSizeSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=object-size
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-ObjectSizeSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=object-size
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  IntegerSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-IntegerSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=integer
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-IntegerSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=integer
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}

  FloatDivideByZeroSan:
    name: ${{ matrix.label }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Autotools-ubuntu-aarch64-clang-FloatDivideByZeroSan
            buildtool: autotools
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --enable-sanitizer=float-divide-by-zero
              --without-png
              --without-jpeg
              --without-tiff
              --without-webp
              --without-librsvg
              --without-gd
              --without-gdk-pixbuf2
              --without-lcms2
              --without-libcurl
              --without-libfetch
            extra_packages: >-
              clang
              libclang-rt-dev
            exec_type: bash

          - label: Meson-ubuntu-aarch64-clang-FloatDivideByZeroSan
            buildtool: meson
            setup_prefix: >-
              CC=clang
            setup_options: >-
              --buildtype=debugoptimized
              -Dexperimental_sanitizer=float-divide-by-zero
              -Db_lundef=false
              -Db_ndebug=false
              -Dpng=disabled
              -Djpeg=disabled
              -Dtiff=disabled
              -Dwebp=disabled
              -Dlibrsvg=disabled
              -Dgd=disabled
              -Dgdk_pixbuf2=disabled
              -Dlcms2=disabled
              -Dcurl=disabled
              -Dfetch=disabled
            extra_packages: >-
              meson
              ninja-build
              clang
              libclang-rt-dev
            exec_type: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run CI steps
        uses: ./.github/actions/ci-steps
        with:
          matrix_json: ${{ toJSON(matrix) }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
